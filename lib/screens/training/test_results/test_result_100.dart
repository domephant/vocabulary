import 'package:flutter/material.dart';

class TestResult100Screen extends StatelessWidget {
  const TestResult100Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
