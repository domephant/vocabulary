import 'package:flutter/material.dart';

class TestResult90Screen extends StatelessWidget {
  const TestResult90Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
