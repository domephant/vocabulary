import 'package:flutter/material.dart';

class TestResult25Screen extends StatelessWidget {
  const TestResult25Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
